{
  "name": "enki-agents",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "type-check": "npm run types && tsc",
    "test": "vitest run",
    "test:unit": "vitest run --config vitest.config.ts",
    "test:integration": "vitest run --config vitest.workers.config.ts",
    "test:e2e": "bash tests/e2e/run-e2e.sh",
    "test:e2e:prod": "bash tests/e2e/run-e2e.sh https://agents.enki.cr"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.2.1",
    "@langchain/langgraph-checkpoint": "^1.0.0",
    "@langchain/openai": "^1.2.12",
    "agents": "^0.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.20",
    "typescript": "^5.8",
    "vitest": "^3.2.4",
    "wrangler": "^4"
  }
}
